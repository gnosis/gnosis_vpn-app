 ---
  In /home/este/rep/gnosis_vpn-app, a new "Reconnecting" VPN status was just added to deriveVPNStatus in src/utils/status.ts (inside the "Running" branch, before the connected check). Now fix all the places that are missing handling for this new status:

  Critical — wrong UI shown:

  1. src/components/status/StatusIndicator.tsx:18-42 — add a "Reconnecting" case before the else block returning { text: "Reconnecting", color: "bg-vpn-yellow" }.
  2. src/components/status/StatusHero.tsx:10 — isConnecting() only checks "Connecting"; change it to also return true for "Reconnecting" so the spinner shows during reconnect.
  3. src/components/status/StatusLine.tsx:38 — connectorClass() has no "Reconnecting" branch; treat it the same as "Connecting" (return "vpn-connector-line connecting").

                                                                                        High — wrong button/list state:

  4. src/components/ConnectButton.tsx:12-13 — isActive() only checks "Connected" || "Connecting"; add "Reconnecting" so the button shows "Disconnect" during reconnect.                                                                                          5. src/components/exitNode/ExitNodeList.tsx:103-107 — vpnActive() checks "Connected", "Connecting", and "Disconnecting"; add "Reconnecting" to the condition.

  Medium — missing reconnect info in status line:

  6. src/components/status/ConnectionStatus.tsx:15-43 — deriveStatus() reads appState.connecting but not appState.reconnecting. Add a branch for appState.reconnecting (same shape as the connecting branch: show phase label or "Reconnecting to {label}").

  App.tsx is fine as-is — its four vpnStatus checks are for update-check and auto-connect logic where "Reconnecting" correctly falls through.

  After all edits, run nix develop --command deno lint --fix then commit with a conventional commit message.
