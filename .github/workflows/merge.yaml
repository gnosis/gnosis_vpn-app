name: Merge PR
on:
  pull_request:
    types:
      - closed
      # - synchronize
    branches:
      - main
concurrency:
  group: merge
  cancel-in-progress: false
jobs:
  build-binaries:
    strategy:
      matrix:
        binary:
          - architecture: universal-darwin
            runner: depot-macos-15 # Depot macOS 15, Apple Silicon (M2), 8 CPUs, 24 GB RAM
          - architecture: aarch64-darwin
            runner: depot-macos-15 # Depot macOS 15, Apple Silicon (M2), 8 CPUs, 24 GB RAM
          - architecture: x86_64-darwin
            runner: depot-macos-15 # Depot macOS runners are Apple Silicon; x86_64 is built via cross-compilation target
          - architecture: x86_64-linux
            runner: depot-ubuntu-22.04-4 # Depot Ubuntu 22.04, 4 CPUs, 16 GB RAM
          - architecture: aarch64-linux
            runner: depot-ubuntu-22.04-arm-4 # Depot Ubuntu 22.04 ARM, 4 CPUs, 16 GB RAM
    name: Binary ${{ matrix.binary.architecture }}
    uses: ./.github/workflows/build-binaries.yaml
    with:
      branch: ${{ github.event.pull_request.base.ref }}
      architecture: ${{ matrix.binary.architecture }}
      runner: ${{ matrix.binary.runner }}
      version_type: "pr"
    secrets: inherit
